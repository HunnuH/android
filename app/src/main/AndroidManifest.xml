<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.advancedview">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdvancedView">

        <activity android:name=".sqllite.DBMainActivity" />
        <activity android:name=".task.Main_menuActivity" />
        <activity android:name=".task.LoginActivity" />
        <activity android:name=".sqllite.ListViewActivity" />
        <activity android:name=".sqllite.ResourceDataListActivity" />
        <activity android:name=".sqllite.SimpleAdapterTestActivity" />
        <activity android:name=".sqllite.SpinnerAdapterTestActivity"/>
        <activity android:name=".exam.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>